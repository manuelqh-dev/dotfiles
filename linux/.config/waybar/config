{
  "layer": "top",
  "position": "top",
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["clock"],
  "modules-right": ["bluetooth", "network", "pulseaudio","cpu", "memory"],
  "spacing": 10,
  "margin": "10 10 0 10",


  "clock": {
    "format": "{:%a %d %b %Y  %H:%M:%S}"
  },

 "bluetooth": {
    "format": "",
    "tooltip-format": "{status} {device_alias}",
    "on-click": "bluetoothctl power on || bluetoothctl power off",
    "on-click-right": "bluetoothctl scan on"
},

  "network": {
    "interface": "wlp2s0",
    "format-wifi": "",
    "format-ethernet": "",
    "format-disconnected": "",
    "tooltip-format": "{ifname} - {essid} ({signalStrength}%)"
  },

  "pulseaudio": {
    "format": "",
    "format-muted": "",
    "tooltip-format": "Volumen: {volume}%",
    "scroll-step": 5
  },

  "cpu": {
    "interval": 2,
    "format": "", // icono CPU
    "tooltip-format": "CPU: {usage}%",
    "max-length": 3
  },

  "memory": {
    "interval": 5,
    "format": "",
    "tooltip-format": "RAM usada: {used} / {total} ({percentage}%)"
  },

"hyprland/workspaces": {
  // Permite usar scroll en el módulo para cambiar workspace (false = activado)
  "disable-scroll": false,

  // Muestra los workspaces en todas las pantallas conectadas
  "all-outputs": true,

  // Muestra solo el icono de cada workspace en la barra
  "format": "{icon}",

  // Al hacer clic en un workspace, se activa ese workspace
  "on-click": "activate",

  // Define workspaces que siempre están activos (persistentes) en todas las pantallas
  "persistent-workspaces": {
    "*": [1, 2, 3, 4, 5, 6, 7, 8, 9]
  },

  // Mapeo de iconos para cada workspace
  "format-icons": {
      "1": "",  
      "2": "", 
      "3": "",  
      "4": "",  
      "5": "",   
      "6": "",  
      "7": "",  
      "8": "",  
      "9": ""
  }
}

}
